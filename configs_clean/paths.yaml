project_root: .

data:
  root: data
  raw: data/raw
  processed: data/processed_clean
  partitions: data/partitions_clean

results:
  root: results
  figures: results/figures/clean
  metrics: results/metrics
  models: results/models
  tensorboard: results/tensorboard

notebooks:
  root: notebooks

docker:
  compose_file: docker/compose.yaml
