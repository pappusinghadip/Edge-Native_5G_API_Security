project_root: .
data:
  root: data
  raw: data/raw
  processed: data/processed_perm
  partitions: data/partitions_perm
results:
  root: results
  figures: results/figures/perm
  metrics: results/metrics
  models: results/models
  tensorboard: results/tensorboard
notebooks:
  root: notebooks
docker:
  compose_file: docker/compose.yaml
